git bash 명령어     이름에 띄어쓰기있으면 따옴표로 묶음

git init -> 해당 폴더를 깃허브와 연동할수 있게 초기화(숨긴 폴더 .git 생성)
git remote add NAME ADDRESS -> 원격저장소 주소를 NAME으로 쓸 수 있게 추가함
	git remote remove NAME -> NAME인 저장소 지움

--초기화부분--

git status -> 현재폴더(pwd)에서 git commit저장소와 맞는지? 추적



branch